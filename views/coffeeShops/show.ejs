<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Show</title>
</head>
<body>
    <header>
        <nav>
            <ul id="nav-links">
                <li><a href="/"> Homepage </a></li>
                <li><a href="/coffeeShops">Back to Coffee Shops</a></li>
                <li><a href="/coffeeShops/<%= coffeeShop._id %>/edit">Edit <%= coffeeShop.name %></a></li>
            </ul>
        </nav>
    </header>
    <main id="coffee-shop-details">
        <h1 id="coffee-shop-title"><%= coffeeShop.name %></h1>

        <% if (coffeeShop.image) { %>
            <img id="coffee-shop-image" src="<%= coffeeShop.image %>" alt="<%= coffeeShop.name %>">
        <% } %>

        <p id="coffee-shop-description"><%= coffeeShop.description %></p>

        <p><strong>Address:</strong> <%= coffeeShop.location %></p>

        <% if (coffeeShop.goodForStudying) { %>
            <p class="study-spot good">📖 This coffee shop is a great spot to study!</p>
        <% } else { %>
            <p class="study-spot bad">☕ This coffee shop is only good for a sip, can't study here!</p>
        <% } %>

        <form action="/coffeeShops/<%=coffeeShop._id%>?_method=DELETE" method="POST">
            <button id="delete-btn" type="submit">🗑️ Delete <%= coffeeShop.name %></button>
        </form>
    </main>
</body>
</html>